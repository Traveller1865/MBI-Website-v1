// components/ui/chart.js
// Bare minimum to satisfy the import and named export

export class Chart {
    constructor(ctx, config) {
      // Placeholder implementation
      this.ctx = ctx
      this.config = config
    }
  }
  
  Chart.defaults = {
    font: {
      family: "'Inter', sans-serif",
    },
    color: "#6c757d",
  }
  
  